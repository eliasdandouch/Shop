name: Shop
version: '${project.version}'
main: louie.shop.Shop
api-version: 1.20
depend: [Bank]
commands:
  test:
    description: test command
  sell:
    description: Allows users to sell
  shop:
    description: Lists all items in the server shop
  
